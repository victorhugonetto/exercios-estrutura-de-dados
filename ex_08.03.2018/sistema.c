#include <stdio.h>
#include <stdlib.h>
#include "aluno.c"

int main() {
	Livro * livro, livros[10]; 
	char titulo[50];
	float preco;
	int nrpng, ano;
	for(contador=0;contador<10;contador++){
		printf("Titulo: ");
		fflush(stdin);
		gets(Titulo);
		printf("Numero de paginas: ");
		scanf("%f",&Paginas);
		printf("Ano: ");
		scanf("%f",&Ano);
		printf("Preco: ");
	    scanf("%f",&Preco);
		Livro = criar(,notaDoAluno);
		turma[contador] = *aluno;
	}
	soma=0;
	for(contador=0;contador<10;contador++){
		livro = &livro[contador];
		void acessar(Livro *livro, char *titulo, float *preco);
		printf("\nnome do livro: %s, preco: %f",TituloDoLivro,PrecoDoLivro);
		soma = soma + notaDoAluno;
	}
	media = soma/10;
	printf("\n\nMedia = %.2f",media);
	return 0;
